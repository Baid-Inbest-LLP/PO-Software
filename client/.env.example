# Local dev: must match server/.env PORT (Vite proxy target)
VITE_API_PORT=5003

# Production (Vercel): backend URL — /api is appended automatically at build time
# VITE_API_BASE_URL=https://your-backend-project.vercel.app
