# Local dev: Vite proxies /api → server (port 5003). Optional override:
# VITE_API_URL=http://localhost:5003/api/v1

# Production (Render): full API base URL at build time
# VITE_API_URL=https://your-backend.onrender.com/api/v1
